let DEBUG_MODE,ValidationError;_b4d‍.x([["default",()=>_b4d‍.o]]);_b4d‍.w("../config",[["DEBUG_MODE",["DEBUG_MODE"],function(v){DEBUG_MODE=v}]]);_b4d‍.w("joi",[["ValidationError",["ValidationError"],function(v){ValidationError=v}]]);

const errorHandler = (err, req, res, next) => {
  let statusCode = 500;

  let data = {
    message: "Internal server error",
    ...(DEBUG_MODE === "true" && { originalError: err.message }),
  };

  if (err instanceof ValidationError) {
    statusCode = 422;
    data = {
      message: err.message,
    };
  }
  return res.status(statusCode).json(data);
};

_b4d‍.d(errorHandler);
